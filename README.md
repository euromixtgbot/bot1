# Bot1 - Telegram Бот з Інтеграцією Jira

🤖 **Telegram бот для безшовного управління задачами Jira та автентифікації користувачів**

[![GitHub](https://img.shields.io/badge/GitHub-euromixtgbot%2Fbot1-blue?logo=github)](https://github.com/euromixtgbot/bot1)
[![Python](https://img.shields.io/badge/Python-3.8%2B-blue?logo=python)](https://python.org)
[![Telegram](https://img.shields.io/badge/Telegram-Bot-blue?logo=telegram)](https://core.telegram.org/bots)

## 🚀 Основні Можливості

### 🎯 **Ключові Функції**
- ✅ **Створення задач Jira через звичайні кнопки** з розумною валідацією полів
- ✅ **Автоматичні коментарі у реальному часі** для відкритих задач
- ✅ **Підтримка прикріплення файлів** з автоматичною інтеграцією Jira
- **📋 Детальніше:** [deployment/README.md](deployment/README.md)

## 💾 Backup та Відновлення

### 🗄️ Організовані Backup Архіви
- **📂 Backup інформація:** [backups/BACKUP_INFO_ORGANIZED_SYSTEM_SEPTEMBER_2025.md](backups/BACKUP_INFO_ORGANIZED_SYSTEM_SEPTEMBER_2025.md)
- **⚡ Швидка довідка:** [backups/QUICK_BACKUP_INFO.md](backups/QUICK_BACKUP_INFO.md)

Три типи backup архівів:
- 🏆 **Організована система** (562 KB) - рекомендовано для production
- 🔄 **Повний backup** (11 MB) - включає venv та логи
- 📦 **Тільки код** (136 KB) - мінімальний для розробки

## 📊 Система Моніторингу

### 🛡️ Автоматичний моніторинг та перезапуск
Всі інструкції та скрипти моніторингу знаходяться в папці **[monitoring/](monitoring/)**:

- 🎯 **[Головний моніторинг](monitoring/monitor_bot.sh)** - перевірка кожні 15 хв, автоперезапуск
- 🚀 **[Автоналаштування](monitoring/setup_monitoring.sh)** - одна команда, все готово
- 🎛️ **[Інтерфейс управління](monitoring/monitor_control.sh)** - зручне меню
- 📚 **[Документація](monitoring/MONITORING_README.md)** - повний опис системи

### ⚡ Швидке налаштування:
```bash
# Автоматичне налаштування моніторингу
sudo /home/Bot1/monitoring/setup_monitoring.sh

# Інтерактивне управління
sudo /home/Bot1/monitoring/monitor_control.sh
```

### 🎯 Що забезпечує моніторинг:
- ✅ Автоматична перевірка стану бота кожні 15 хвилин
- ✅ Автоматичний перезапуск при збоях (до 3 спроб)
- ✅ Systemd інтеграція з автозапуском після перезавантаження
- ✅ Детальне логування всіх операцій
- ✅ Перезапуск сервера при критичних помилках

**📋 Детальніше:** [monitoring/README.md](monitoring/README.md)

## 📞 Підтримка**Автентифікація користувачів** через номери телефонів
- ✅ **Інтеграція з Google Sheets** для управління користувачами
- ✅ **Повний процес реєстрації** для нових користувачів
- ✅ **Звичайні кнопки замість inline** для кращого UX
- ✅ **Захист від дублювання задач** при наявності відкритих

### 🔧 **Останні Покращення (серпень 2025)** 
- 🆕 **Конвертовано з inline на звичайні кнопки** для покращення користувацького досвіду
- 🆕 **Виправлено зациклення вибору сервісу** через конфлікти ConversationHandler
- 🆕 **Додано валідацію створення задач** - заборона при наявності відкритих задач
- 🆕 **Покращено навігацію кнопок** з додаванням кнопки "Назад"
- 🆕 **Стабілізовано систему коментарів** з автоматичним виявленням стану задач

## � Сценарії Використання Бота ("Якщо-То")

### 🎯 **Перший запуск бота**

**ЯКЩО** користувач вперше запускає бота (`/start`)  
**ТО** система:
1. Перевіряє користувача в Google Sheets за Telegram ID
2. Якщо знайдений → автоматично авторизує і показує головне меню
3. Якщо не знайдений → запитує номер телефону для авторизації
4. Якщо номер знайдений в базі → авторизує та оновлює Telegram ID
5. Якщо номер не знайдений → запускає процес реєстрації нового користувача

---

### 🔐 **Сценарії Авторизації**

**ЯКЩО** користувач натискає "📞 Надати номер телефону"  
**ТО** система:
1. Отримує контакт через Telegram API
2. Валідує формат українського номера (+380XXXXXXXXX)
3. Шукає номер в Google Sheets
4. При знаходженні → завершує авторизацію
5. При відсутності → переходить до реєстрації

**ЯКЩО** користувач натискає "👤 Продовжити без авторизації"  
**ТО** система:
1. Дозволяє створювати задачі без попередньої авторизації
2. При створенні задачі збирає дані користувача (ПІБ, підрозділ, департамент)
3. Зберігає дані в Google Sheets для майбутніх звернень

**ЯКЩО** користувач натискає "🏠 Вийти на головну"  
**ТО** система:
1. Завершує поточний процес авторизації/реєстрації
2. Очищає тимчасові дані
3. Повертає до головного меню

---

### 🆕 **Сценарії Створення Задач**

**ЯКЩО** користувач натискає "🆕 Створити задачу"  
**ТО** система:
1. Перевіряє наявність відкритих задач у користувача
2. При наявності відкритої задачі → блокує створення і показує існуючу
3. При відсутності → запускає процес створення через ConversationHandler

**ЯКЩО** користувач в стані AUTHORIZED_NO_TASKS пише текстове повідомлення  
**ТО** система:
1. Автоматично використовує текст як опис нової задачі
2. Запускає швидке створення задачі з мінімальними кроками

#### **Крок 1: Вибір сервісу**
**ЯКЩО** користувач натискає назву сервісу (E-mix 2.x, E-drive, Пошта, тощо)  
**ТО** система:
1. Зберігає вибраний сервіс
2. Переходить до запиту опису проблеми

**ЯКЩО** користувач натискає "🔙 Назад"  
**ТО** система:
1. Повертається до попереднього кроку в ConversationHandler
2. Відновлює відповідну клавіатуру

#### **Крок 2: Опис проблеми**
**ЯКЩО** користувач пише текст  
**ТО** система:
1. Зберігає текст як опис задачі
2. Показує підтвердження створення з inline кнопками

**ЯКЩО** користувач надсилає фото з підписом  
**ТО** система:
1. Використовує підпис як опис задачі
2. Зберігає фото для прикріплення до задачі
3. Показує підтвердження створення

#### **Крок 3: Підтвердження**
**ЯКЩО** користувач натискає "Створити задачу"  
**ТО** система:
1. Створює задачу в Jira з зібраними даними
2. Прикріпляє фото (якщо було надіслано)
3. Встановлює задачу як активну для користувача
4. Змінює стан користувача на AUTHORIZED_WITH_TASK
5. Повідомляє про успішне створення з номером задачі

**ЯКЩО** користувач натискає "🔙 Назад"  
**ТО** система:
1. Повертається до вибору сервісу
2. Зберігає всі введені дані

---

### 💬 **Сценарії Роботи з Коментарями**

**ЯКЩО** користувач в стані AUTHORIZED_WITH_TASK пише текст  
**ТО** система:
1. Перевіряє статус активної задачі
2. Якщо задача відкрита → додає текст як коментар до Jira
3. Якщо задача закрита → повідомляє про неможливість коментування
4. Очищає активну задачу і змінює стан на AUTHORIZED_NO_TASKS

**ЯКЩО** користувач надсилає файл (фото/документ) з підписом  
**ТО** система:
1. Завантажує файл на Google Drive
2. Додає коментар з текстом підпису
3. Додає окремий коментар з посиланням на файл

**ЯКЩО** користувач надсилає файл без підпису  
**ТО** система:
1. Завантажує файл на Google Drive
2. Додає коментар тільки з посиланням на файл

---

### 🧾 **Сценарії Перегляду Задач**

**ЯКЩО** користувач натискає "🧾 Мої задачі"  
**ТО** система:
1. Шукає відкриті задачі користувача в Jira
2. Відображає список у текстовому форматі (не inline кнопки)
3. Показує клавіатуру з опціями оновлення та виходу

**ЯКЩО** користувач натискає "🔄 Оновити статус задачі"  
**ТО** система:
1. Повторно запитує Jira API для актуального статусу
2. Оновлює відображення списку задач
3. Якщо задачі стали закритими → очищає активну задачу користувача

**ЯКЩО** користувач натискає "✅ Перевірити статус задачі"  
**ТО** система:
1. Отримує детальну інформацію про активну задачу
2. Показує статус, опис та дату останнього оновлення

---

### 🏠 **Сценарії Навігації**

**ЯКЩО** користувач натискає "🏠 Вийти на головну" в будь-якому стані  
**ТО** система:
1. Завершує поточний ConversationHandler
2. Очищає тимчасові дані
3. Повертає до головного меню зі стандартними кнопками

**ЯКЩО** користувач натискає "🔄 Повторити /start"  
**ТО** система:
1. Зберігає поточний профіль користувача
2. Викликає функцію start() повторно
3. Відновлює головне меню без втрати авторизації

**ЯКЩО** користувач натискає "🔄 Повторна авторизація"  
**ТО** система:
1. Очищає всі дані користувача
2. Запускає процес авторизації спочатку
3. Запитує номер телефону або реєстрацію

---

### ⚠️ **Сценарії Обробки Помилок**

**ЯКЩО** користувач намагається створити задачу при наявності відкритої  
**ТО** система:
1. Блокує створення нової задачі
2. Показує інформацію про існуючу відкриту задачу
3. Пропонує додати коментар до існуючої або дочекатися закриття

**ЯКЩО** користувач надсилає некоректний номер телефону  
**ТО** система:
1. Валідує формат номера
2. Повідомляє про помилку з поясненням правильного формату
3. Просить ввести номер повторно

**ЯКЩО** виникає помилка при створенні задачі в Jira  
**ТО** система:
1. Логує помилку для адміністратора
2. Повідомляє користувача про технічну проблему
3. Повертає до головного меню без зміни стану

**ЯКЩО** користувач надсилає коментар до закритої задачі  
**ТО** система:
1. Перевіряє статус задачі в Jira
2. Повідомляє про неможливість коментування закритої задачі
3. Очищає активну задачу і пропонує створити нову

---

### 🔧 **Системні Сценарії**

**ЯКЩО** отримано webhook від Jira про зміну статусу задачі  
**ТО** система:
1. Знаходить користувача за Telegram ID в задачі
2. Надсилає повідомлення про зміну статусу
3. Якщо задача закрита → очищає активну задачу користувача

**ЯКЩО** відбувається конфлікт між ConversationHandler та основними обробниками  
**ТО** система:
1. Перевіряє наявність активного Conversation
2. Блокує обробку через main_message_dispatcher
3. Передає управління ConversationHandler

**ЯКЩО** користувач натискає кнопку головного меню під час Conversation  
**ТО** система:
1. Виявляє конфлікт через check_main_menu_button_and_exit()
2. Завершує ConversationHandler
3. Очищає дані Conversation
4. Повертає до головного меню

## 📋 Детальні Можливості

### 🛠️ **Обробка Помилок та Валідація**

Бот розумно обробляє помилки валідації полів Jira:

1. **Користувацькі Поля** (підрозділ, департамент, сервіс) - автоматично видаляються з запиту при невдалій валідації
2. **Поле Репортер** - автоматично замінюється на `JIRA_REPORTER_ACCOUNT_ID` при помилці валідації користувача
3. **Логіка Повторних Спроб** - до 4 спроб з поступовим видаленням полів для успішного створення задачі
4. **Захист від дублювання** - перевірка відкритих задач перед створенням нової

### 💬 **Управління Коментарями**

Після створення задачі:
1. **Автоматичний Режим Коментарів** - безшовно активується після створення задачі
2. **Текстові Повідомлення** - просто введіть текст і він стане коментарем до вашої активної задачі
3. **Прикріплення Файлів** - надішліть файли або фото, які автоматично прикріпляться до задач Jira
4. **Атрибуція Автора** - коментарі включають правильну ідентифікацію користувача та форматування
5. **Перевірка Статусу** - коментарі блокуються для закритих задач

### 📁 **Обробка Файлів**

Підтримуються різні типи файлів:
- 📷 **Фотографії** (JPG, PNG) - автоматично прикріпляються з попереднім переглядом
- 📄 **Документи** (PDF, DOC, DOCX, TXT) - прикріпляються з посиланням
- 🎥 **Відео файли** (MP4, AVI) - з автоматичним стисненням
- 🎵 **Аудіо файли** (MP3, WAV) - з метаданими

### 👤 **Система Автентифікації**

**Автоматична Автентифікація:**
- Пошук користувача за Telegram ID
- Автоматичне оновлення username при зміні

**Автентифікація через Телефон:**
- Валідація українських номерів (+380XXXXXXXXX)
- Інтеграція з Google Sheets для перевірки користувачів
- Автоматичне оновлення даних при успішній автентифікації

**Реєстрація Нових Користувачів:**
- Збір повного імені (ПІБ)
- Вибір підрозділу та департаменту
- Підтвердження даних перед збереженням
- Автоматичне створення запису в Google Sheets

### 🔄 **Система Станів Користувачів**

Бот підтримує три основні стани:
1. **REGISTRATION** - процес реєстрації або авторизації
2. **AUTHORIZED_NO_TASKS** - авторизований без відкритих задач
3. **AUTHORIZED_WITH_TASK** - авторизований з активною задачею

**Переходи між станами:**
- Після успішної авторизації → AUTHORIZED_NO_TASKS
- Після створення задачі → AUTHORIZED_WITH_TASK
- Після закриття задачі → AUTHORIZED_NO_TASKS

## 🎮 Команди та Інтерфейс

### 📱 **Основні Команди**
```
/start - Запуск бота та автентифікація
/cancel - Скасування поточної операції (у ConversationHandler)
```

### 🎯 **Головне Меню (Звичайні Кнопки)**
- **🆕 Створити задачу** - Запуск процесу створення нової задачі через ConversationHandler
- **🧾 Мої задачі** - Перегляд списку відкритих задач у текстовому форматі
- **ℹ️ Допомога** - Інформація про використання бота
- **🔄 Повторити /start** - Перезапуск бота зі збереженням профілю

### 🔐 **Кнопки Авторизації**
- **📞 Надати номер телефону** - Авторизація через Telegram контакт
- **� Продовжити без авторизації** - Створення задач без попередньої авторизації
- **🏠 Вийти на головну** - Повернення до головного меню (доступна у всіх станах)
- **🔄 Повторна авторизація** - Очищення профілю та нова автентифікація

### 🛠️ **Кнопки Створення Задач (ConversationHandler)**
#### Вибір Сервісу:
- **E-mix 2.x** / **E-mix 3.x** / **E-supervisor** / **E-mix market** / **E-mix market iOS**
- **E-drive** / **Пошта** / **Портал техпідтримки**
- **🔙 Назад** - Повернення до попереднього кроку

#### Підтвердження Створення (Inline кнопки):
- **Створити задачу** - Фінальне підтвердження створення
- **🔙 Назад** - Повернення до редагування опису

### 🧾 **Кнопки Режиму Перегляду Задач**
- **🔄 Оновити статус задачі** - Перевірка актуального статусу задач у Jira
- **✅ Перевірити статус задачі** - Детальна інформація про активну задачу
- **🏠 Вийти на головну** - Повернення до головного меню

### 💬 **Автоматичні Дії за Контекстом**
- **Текстові повідомлення у стані AUTHORIZED_NO_TASKS** → Автоматичне створення нової задачі
- **Текстові повідомлення у стані AUTHORIZED_WITH_TASK** → Автоматичні коментарі до активної задачі
- **Файли з підписом** → Створення коментаря з текстом + окремий коментар з файлом
- **Файли без підпису** → Створення коментаря тільки з посиланням на файл
- **Фото під час створення задачі** → Автоматичне прикріплення до нової задачі

## 🔧 Системні Команди (для адміністраторів)

### 🚀 **Запуск та Зупинка**
```bash
# Запуск бота
python src/main.py

# Запуск через скрипт (рекомендовано)
./activate_and_run.sh

# Перезапуск бота
./restart.sh

# Зупинка бота (знайти PID та завершити процес)
ps aux | grep main.py
kill <PID>
```

### 📊 **Моніторинг та Логи**
```bash
# Перегляд логів бота
tail -f logs/bot.log

# Перегляд логів webhook
tail -f logs/webhook.log

# Перегляд всіх активних процесів бота
ps aux | grep python | grep main.py

# Швидка перевірка системи
./quick_check.sh

# Повна перевірка системи
./final_check.sh
```

### 🔄 **Управління Станом**
```bash
# Очищення файлів стану користувачів
rm -rf user_states/*

# Перегляд активних станів
ls -la user_states/

# Створення резервної копії
cd /home && tar -czf Bot1_backup_$(date +%Y%m%d_%H%M%S).tar.gz --exclude='Bot1/venv' Bot1/
```

### 📦 **Управління Залежностями**
```bash
# Встановлення залежностей
pip install -r requirements.txt

# Оновлення залежностей
pip list --outdated
pip install --upgrade package_name

# Створення віртуального середовища
python -m venv venv
source venv/bin/activate
```

## 🏗️ Архітектура Системи

### 📂 **Структура Проекту**
```
Bot1/
├── src/                           # Основний код застосунку
│   ├── main.py                   # Точка входу бота
│   ├── handlers.py               # Обробники повідомлень, команд та ConversationHandler
│   ├── services.py               # Сервіси для роботи з Jira API
│   ├── keyboards.py              # Визначення звичайних та inline клавіатур
│   ├── constants.py              # Константи системи (сервіси, підрозділи, ID маппінги)
│   ├── google_sheets_service.py  # Інтеграція з Google Sheets
│   ├── user_state_service.py     # Управління станом користувачів
│   ├── user_management_service.py # Гібридний сервіс пошуку користувачів
│   ├── jira_webhooks2.py         # Обробка webhook від Jira
│   ├── field_mapping.py          # Маппінг полів між системами
│   └── fixed_issue_formatter.py  # Форматування відображення задач
├── config/                       # Конфігураційні файли
│   ├── config.py                 # Основна конфігурація
│   ├── credentials.env           # Змінні середовища (не в Git)
│   ├── service_account.json      # Ключі Google API (не в Git)
│   └── fields_mapping.yaml       # Маппінг полів Jira
├── logs/                         # Файли логування
│   ├── bot_*.log                 # Основні логи бота
│   ├── webhook.log               # Логи webhook обробки
│   └── conflict_errors.log       # Логи конфліктних помилок
├── user_states/                  # Стани користувачів (JSON файли)
├── backups/                      # Резервні копії проекту
├── Tests/                        # Тести та діагностика
├── scripts/                      # Допоміжні скрипти
└── requirements.txt              # Python залежності
```

### 🔄 **Потік Роботи Системи**

1. **Ініціалізація:**
   - Завантаження конфігурації з `config/config.py`
   - Підключення до Telegram Bot API
   - Налаштування webhook для Jira
   - Реєстрація обробників у правильному порядку

2. **Обробка Повідомлень:**
   - **ConversationHandler** (найвищий пріоритет) для створення задач
   - **Кнопочні обробники** для конкретних дій
   - **main_message_dispatcher** для контекстної обробки на основі стану
   - **Глобальні обробники** для файлів та контактів

3. **Автентифікація Користувача:**
   - Гібридний пошук через `user_management_service`
   - Перевірка за Telegram ID в локальному кеші та Google Sheets
   - Запит номера телефону при необхідності
   - Реєстрація нового користувача з валідацією

4. **Створення Задачі (ConversationHandler):**
   - Збір інформації поетапно: сервіс → опис → підтвердження
   - Валідація та маппінг полів Jira через `constants.py`
   - Створення задачі з обробкою помилок та повторними спробами
   - Прикріплення файлів та фото

5. **Обробка Коментарів:**
   - Автоматичне виявлення на основі стану користувача
   - Перевірка статусу задачі перед додаванням коментаря
   - Завантаження файлів на Google Drive з генерацією посилань
   - Форматування коментарів з ідентифікацією автора

### 🔧 **Ключові Компоненти**

#### **ConversationHandler**
```python
States: SERVICE → DESCRIPTION → CONFIRM
Entry: MessageHandler(filters.Regex("^🆕 Створити задачу$"))
Fallbacks: /start, /cancel
Exit: "🏠 Вийти на головну" в будь-якому стані
```

#### **Message Dispatcher**
```python
Priority Groups:
- Group 0: ConversationHandler
- Group 1: Button handlers (🆕, 🧾, ℹ️, 🔄)
- Group 2: main_message_dispatcher (context-aware)
- Group 3: Global handlers (files, contacts)
```

#### **User State Management**
```python
States:
- REGISTRATION: реєстрація/авторизація
- AUTHORIZED_NO_TASKS: авторизований без задач
- AUTHORIZED_WITH_TASK: авторизований з активною задачею

Persistence: JSON files in user_states/
```

#### **Error Handling & Validation**
```python
Jira Field Validation:
1. Attempt with all custom fields
2. Remove custom fields on validation error
3. Fallback to basic fields only
4. Use default reporter account if needed
```

## 🔐 Безпека та Конфігурація

### 🛡️ **Налаштування Безпеки**
```bash
# Налаштування прав доступу до конфігураційних файлів
chmod 600 config/credentials.env
chmod 600 config/service_account.json

# Створення користувача для бота
sudo useradd -m -s /bin/bash telegrambot
sudo chown -R telegrambot:telegrambot /home/Bot1
```

### ⚙️ **Змінні Середовища**
```env
# config/credentials.env
TELEGRAM_BOT_TOKEN=your_bot_token_here
JIRA_URL=https://your-domain.atlassian.net
JIRA_EMAIL=your-email@domain.com
JIRA_API_TOKEN=your_jira_api_token
JIRA_PROJECT_KEY=YOUR_PROJECT_KEY
GOOGLE_SHEETS_ID=your_sheets_id_here
```

### 🔧 **Systemd Сервіс**
```bash
# Встановлення як системний сервіс
sudo cp telegram-bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot

# Перевірка статусу
sudo systemctl status telegram-bot
```

## 🧪 Тестування та Діагностика

### 🔍 **Діагностичні Скрипти**
```bash
# Швидка перевірка основних компонентів
./quick_check.sh

# Повна діагностика системи
./final_check.sh

# Тестування з'єднання з Jira
python Tests/test_jira_connection.py

# Перевірка Google Sheets інтеграції
python Tests/test_sheets_connection.py
```

### 📊 **Моніторинг Продуктивності**
```bash
# Моніторинг використання пам'яті
ps aux | grep python | awk '{print $2, $4, $11}' | grep main.py

# Розмір файлів логів
du -sh logs/*

# Кількість активних користувачів
ls user_states/ | wc -l
```

## 🚨 Усунення Неполадок

### ❗ **Поширені Проблеми та Рішення**

**🔴 Бот не відповідає на повідомлення:**
```bash
# Перевірити процес
ps aux | grep main.py
# Перевірити логи
tail -n 50 logs/bot_*.log
# Перезапустити
./restart.sh
```

**🔴 Зациклення при виборі сервісу:**
```bash
# Це було виправлено в серпні 2025
# Перевірити наявність конфліктів ConversationHandler
grep -n "check_main_menu_button_and_exit" src/handlers.py
# Перевірити порядок реєстрації обробників
grep -A 10 "register_handlers" src/handlers.py
```

**🔴 Кнопка "Назад" не працює:**
```bash
# Перевірити handlers для стану SERVICE
grep -A 5 'MessageHandler.*🔙 Назад' src/handlers.py
# Перевірити реєстрацію окремого обробника
grep "Regex.*🔙 Назад" src/handlers.py
```

**🔴 Система дозволяє створювати дублювальні задачі:**
```bash
# Перевірити логіку валідації в create_task_button_handler
grep -A 15 "find_open_issues" src/handlers.py
# Перевірити виведення блокування
grep "відкрита задача" src/handlers.py
```

**🔴 Помилки створення задач в Jira:**
```bash
# Перевірити конфігурацію Jira
python Tests/test_jira_fields.py
# Перевірити маппінг полів
cat config/fields_mapping.yaml
# Перевірити логи валідації
grep "Помилка валідації" logs/bot_*.log
```

**🔴 Проблеми з Google Sheets:**
```bash
# Перевірити права доступу
ls -la config/service_account.json
# Тестувати з'єднання
python Tests/test_sheets_access.py
# Перевірити логи синхронізації
grep "Google Sheets" logs/bot_*.log
```

**🔴 Коментарі не додаються до задач:**
```bash
# Перевірити стан користувача
grep "AUTHORIZED_WITH_TASK" logs/bot_*.log
# Перевірити активну задачу
grep "current_task_key" user_states/<telegram_id>.json
# Перевірити статус задачі в Jira
python Tests/test_jira_status.py <TASK_KEY>
```

### 🔧 **Діагностичні Команди**

**Швидка діагностика системи:**
```bash
./quick_check.sh
```

**Повна діагностика з деталями:**
```bash
./final_check.sh
```

**Перевірка конкретного користувача:**
```bash
# Стан користувача
cat user_states/<telegram_id>.json
# Логи користувача
grep "<telegram_id>" logs/bot_*.log | tail -20
```

**Моніторинг активних процесів:**
```bash
# Активні ConversationHandler
grep "ConversationHandler" logs/bot_*.log | tail -10
# Конфлікти обробників
grep "Conversation.*пропускаємо" logs/bot_*.log | tail -10
```

### 🔍 **Перевірка Інтеграцій**

**Тестування Jira API:**
```bash
python Tests/test_jira_connection.py
python Tests/test_jira_create_issue.py
```

**Тестування Google Sheets:**
```bash
python Tests/test_sheets_connection.py
python Tests/test_user_search.py
```

**Тестування Telegram Bot API:**
```bash
python Tests/test_bot_connection.py
```

## 📈 Розвиток та Розширення

### 🎯 **Нещодавно Реалізовані Функції (серпень 2025)**
- ✅ **Конвертація на звичайні кнопки** - повна заміна inline кнопок на ReplyKeyboardMarkup
- ✅ **Виправлення конфліктів ConversationHandler** - вирішення зациклення при виборі сервісу
- ✅ **Валідація створення задач** - захист від дублювання при наявності відкритих задач
- ✅ **Покращення навігації** - додавання функціональних кнопок "Назад" та "Вийти на головну"
- ✅ **Стабілізація коментарів** - перевірка статусу задач перед додаванням коментарів

### 🎯 **Заплановані Функції**
- [ ] **Мультипроектна підтримка** - робота з різними проектами Jira
- [ ] **Розширена система ролей** - адміністратори, модератори, користувачі
- [ ] **Інтеграція з іншими трекерами** - GitHub Issues, Azure DevOps, ClickUp
- [ ] **Web інтерфейс адміністрування** - управління користувачами та налаштуваннями
- [ ] **Аналітика та звіти** - статистика створення та вирішення задач
- [ ] **Масові операції** - групове оновлення статусів та призначення
- [ ] **Шаблони задач** - попередньо налаштовані типи задач
- [ ] **Нотифікації по розкладу** - регулярні нагадування про відкриті задачі

### 🔗 **API Інтеграції**
- **Jira REST API v3** - Управління задачами, коментарями та вкладеннями
- **Telegram Bot API** - Взаємодія з користувачами через бота
- **Google Sheets API v4** - Управління користувацькими даними та синхронізація
- **Google Drive API** - Зберігання файлових вкладень з публічними посиланнями

### 🧪 **Технічні Покращення**
- **Async/Await Паттерн** - Повна асинхронність для кращої продуктивності
- **Робастна Обробка Помилок** - Градаційна валідація полів Jira з fallback
- **Кешування Користувачів** - Локальне зберігання для швидкого доступу
- **Webhook Система** - Реальний час оновлень статусів задач
- **Логування та Моніторинг** - Детальне відстеження всіх операцій

## � Розгортання на Новому Сервері

### 📦 Інструкції та Скрипти
Всі необхідні інструкції для розгортання знаходяться в папці **[deployment/](deployment/)**:

- 🤖 **[Автоматичне розгортання](deployment/auto_deploy.sh)** - один скрипт, все готово (5 хв)
- ⚡ **[Швидкий гайд](deployment/QUICK_DEPLOY.md)** - ручне розгортання (15 хв)  
- 📖 **[Детальна інструкція](deployment/DEPLOYMENT_GUIDE.md)** - покрокове керівництво (30 хв)
- ✅ **[Чек-лист](deployment/deployment_checklist.md)** - для системних адміністраторів

### ⚡ Найшвидший спосіб:
```bash
wget https://raw.githubusercontent.com/euromixtgbot/bot1/main/deployment/auto_deploy.sh
chmod +x auto_deploy.sh && sudo ./auto_deploy.sh
```

### 🎯 Що отримаєте після розгортання:
- ✅ Повністю налаштований Telegram бот
- ✅ Автоматичний моніторинг та перезапуск
- ✅ Systemd інтеграція з автозапуском
- ✅ Система backup та логування

**📋 Детальніше:** [deployment/README.md](deployment/README.md)

## �📞 Підтримка

Для отримання підтримки або повідомлення про помилки:
- 📧 **Email:** euromixtgbot@gmail.com
- 🐛 **Issues:** [GitHub Issues](https://github.com/euromixtgbot/bot1/issues)
- 📖 **Документація:** Дивіться файли в папці `reports/`

---

**Розроблено з ❤️ для ефективного управління задачами**
