# Розширений аналіз логіки бота та інтеграцій

> **Мета:** детально описати усі сценарії взаємодії користувачів, автоматизацій, API та вебхуків; визначити слабкі місця логіки та відсутні перевірки; підготувати підґрунтя для написання автоматичних тестів.

---

## 1. Архітектурний огляд

- **Telegram бот (`src/main.py`, `src/handlers.py`)**: керує авторизацією, меню, створенням задач, обробкою вкладень та команд користувача.
- **Локальний кеш (`src/user_state_service.py`)**: зберігає стан реєстрації, профілі, статус синхронізації.
- **Google Sheets (`src/google_sheets_service.py`, `src/user_management_service.py`)**: головне джерело істини про користувачів, синхронізується із кешем.
- **Jira API (`src/services.py`)**: створення задач, додавання коментарів/вкладень, пошук задач та користувачів.
- **Jira вебхуки (`src/jira_webhooks2.py`)**: отримує зміни зі служби, пересилає повідомлення та файли у Telegram.
- **Інтерфейси користувача (`src/keyboards.py`)**: головне меню, клавіатури авторизації, перегляду задач та підтвердження.

---

## 2. Сценарії користувача

### 2.1. Вхід у бот

1. **Команда `/start` або кнопка `🔄 Повторити /start`**
    - `handlers.start()` очищає контекст (якщо профіль відсутній), викликає `user_manager.find_user_comprehensive()`.
    - Якщо знайдено профіль:
       - Оновлюється `telegram_username` у Google (за потреби).
       - Перевіряється поточна задача `get_user_current_task()`.
       - Показується вітальне повідомлення з джерелом даних (Google / кеш) та станом задачі.
    - Якщо профіль не знайдено:
       - Пропонується кнопка `📞 Надати номер телефону`.
       - Встановлюється `context.user_data["new_user"]` для подальшої реєстрації.
    - **Валідація:**
       - `check_required_objects()` гарантує наявність `effective_user`, `message`/`callback_query`, `context.user_data`.
       - Будь-яке введення під час авторизації без профілю автоматично спрямовує користувача на етап збору телефону.

2. **Кнопка `🔄 Повторна авторизація`**
   - `re_auth_handler()` видаляє кешовані дані (`delete_user_state`), очищує `user_data`.
   - Повертає користувача у сценарій авторизації.
   - **Валідація:** перевіряє доступність `effective_user`; виклики без авторизованого користувача завершуються поверненням без змін.

### 2.2. Авторизація за номером телефону

1. **Надання контакту через кнопку**
    - `global_contact_handler()` перевіряє присутність обʼєктів Telegram, бере номер телефону.
    - `user_manager.authorize_user()` шукає користувача в Google, оновлює `telegram_id`/`username`, кешує профіль, ставить `sync_with_google=True`.
    - Якщо Google недоступний або користувача немає:
       - Використовується локальний кеш.
       - Або повертається статус `need_registration` для запуску реєстрації.
    - **Валідація:**
       - Обробник відхиляє будь-які повідомлення без `contact` обʼєкта, повертаючи користувачу нагадування про кнопку.
       - Перевірки формату телефону виконує `find_user_by_phone()` (через `_normalize_phone`).

2. **Спроба обійти авторизацію**
   - Будь-яке текстове повідомлення неавторизованого користувача перехоплює `global_awaiting_auth_text_handler()`, нагадуючи про необхідність контакту.
   - Будь-які вкладення блокує `global_awaiting_auth_media_handler()`.
   - **Валідація:** обидва глобальні хендлери перевіряють `context.user_data` на наявність `profile`; якщо користувач не авторизований, усі текстові та медіа-повідомлення видаляються з подальшої обробки, ConversationHandler не запускається.

### 2.3. Реєстрація нового користувача

1. **Збереження стану реєстрації**
   - `save_registration_state()` формує JSON із типом `registration_in_progress` та кроком.
   - `load_registration_state()` перевіряє давність даних (24 години), очищує застарілу інформацію.
   - **Валідація:**
     - При зчитуванні файлів виконується `try/except` з логуванням; при пошкоджених JSON повертається `None`.
     - `datetime.fromisoformat` контролює коректність дати оновлення.

2. **Завершення реєстрації**
   - `complete_registration()` змінює тип запису на `registration_completed`, додає час завершення.
   - `complete_user_registration_and_set_state()` переводить користувача у стан `AUTHORIZED_NO_TASKS`.

3. **Реєстрація в Google**
   - `user_manager.register_new_user()` викликає `add_new_user(record)`.
   - При успіху кеш зберігається з `sync_with_google=True`; при помилці — тільки локально (`sync_with_google=False`).
   - **Валідація:** `add_new_user()` перевіряє наявність хоча б одного обовʼязкового поля (`full_name`, `telegram_id` або `mobile_number`), нормалізує номери телефонів та `telegram_id`.

### 2.4. Головне меню та функції

1. **Перегляд задач (`🧾 Мої задачі`)**
   - `my_issues()` визначає `telegram_id` (із профілю або поточного користувача).
   - Викликає `find_open_issues()`, зберігає список у `user_data`, позначає активну задачу.
   - Якщо задач немає — повертає на головне меню.
   - **Валідація:**
     - `find_open_issues()` виконує запит до Jira; при помилках повертає пустий список, що обробляється окремим повідомленням.
     - Перед відправкою повідомлень перевіряється наявність `update.callback_query` чи `update.message` для коректного відповіді.

2. **Оновлення статусу (`🔄 Оновити статус задачі`)**
   - `update_issues_status()` використовує збережений список або повторно звертається до Jira.
   - Перевіряє задачі зі статусом DONE (`find_done_issues()`), оновлює блоки повідомлень.
   - **Валідація:** у разі відсутності даних про задачі або помилки Jira обробник повертає користувача до головного меню з поясненням.

3. **Перегляд профілю (`👤 Мій профіль`)**
   - Виводить збережені дані користувача, включно з інформацією про синхронізацію.

4. **Додаткові команди**
   - `/cache_status` — `cache_status_handler()` показує інформацію про кеш.
   - `/sync_cache` — `sync_cache_handler()` запускає `user_manager.sync_pending_users()`.
   - `/reset` — `reset_registration_handler()` скидає стан реєстрації.
   - **Валідація:**
     - Кожен handler перевіряє наявність профілю перед виконанням або повертає відповідь про необхідність авторизації.
     - `sync_pending_users()` обробляє виключення Google Sheets, накопичує помилки у списку `errors`.

### 2.5. Створення задачі в Jira

1. **Кнопка `🆕 Створити задачу`**
   - `create_task_button_handler()` перевіряє через `find_open_issues()` наявність активних задач.
   - Якщо задача існує — блокує створення нової, пропонує перегляд.
   - Якщо ні — запускає `ConversationHandler` (`create_issue_start`).
   - **Валідація:** обробник відкидає події без `message` (наприклад, callback), логуючи помилки.

2. **Прохід по кроках ConversationHandler**
   - `FULL_NAME`: збереження ПІБ.
   - `MOBILE_NUMBER`: валідація формату (через `validate_phone_format`).
   - `DIVISION`, `DEPARTMENT`, `SERVICE`: перехід по кнопках із мапінгами (`DIVISION_ID_MAPPINGS`, тощо).
   - `DESCRIPTION`: текст або фото; зберігається у контексті, підтримка вкладень.
   - `CONFIRM`: inline-кнопки `Створити задачу` або `🔙 Назад`.
   - Будь-яка кнопка головного меню в процесі — `check_main_menu_button_and_exit()` очищує дані та виходить.
   - **Валідація:**
     - На кожному кроці перевіряється `check_main_menu_button_and_exit()`; спроби вводити довільний текст замість кнопок відправляють підказки/повідомлення «натисніть кнопку» або перенаправляють на попередній крок.
     - Для контактів приймаються тільки `Message.contact`; текст чи файл на етапі `MOBILE_NUMBER` обробляється функцією `remind_to_use_button()`/`reject_any_action_during_auth()`.
     - Фотографії на етапі опису проходять через `context.user_data["attached_photo"]`; інші типи медіа відхиляються.

3. **Підтвердження**
   - `confirm_callback()` формує `bot_vars`, мапить у Jira поля через `build_jira_payload()`.
   - `_ensure_correct_custom_fields_format()` адаптує кастомні поля, при помилках — поетапно прибирає проблемні значення.
   - `create_jira_issue()` викликає `_make_request()` з повторними спробами.
   - Успішне створення → зберігає `current_task_key` у `set_user_current_task()`.
   - **Валідація:**
     - Inline-кнопки обмежують доступні дії (`CallbackQueryHandler` із pattern).
     - Перед створенням задачі перевіряються обовʼязкові поля (`project`, `issuetype`, `summary`), некоректні значення викликають `ValueError` із логуванням.

### 2.6. Додавання коментарів та файлів

1. **Кнопка `💬 коментар до задачі`**
   - `comment_handler()` визначає активну задачу.
   - Відправляє зібраний текст через `text_comment_handler()` з `add_comment_to_jira()`.
   - **Валідація:**
     - Якщо текст пустий — користувач отримує повідомлення «❌ Порожній коментар».
     - Відсутність активної задачі повертає користувача до перегляду задач.

2. **Вкладення файлів / фото**
   - `file_handler()` ідентифікує активну задачу, завантажує файл, передає до `attach_file_to_jira()`.
   - За потреби додає коментар із референсом (`add_comment_with_file_reference_to_jira()`).
   - **Валідація:**
     - Неавторизовані користувачі та користувачі без активної задачі отримують повідомлення про заборону.
     - Підтримуються лише `Document`, `Photo`, `Video`, `Audio`; інші типи перехоплює `global_awaiting_auth_media_handler()`.

### 2.7. Вихід з перегляду / повернення на головну

- Кнопки `🏠 Вийти на головну` або `🔙 Назад` обробляють `return_to_main()` та `handle_back_button()`, очищуючи активний режим.
- `main_message_dispatcher()` виступає резервним маршрутизатором (перевіряє стан бота, переключає логіку в залежності від `BotState`).
- **Валідація:**
   - Повернення на головну доступне лише через передбачені кнопки; введення тексту «назад» без емодзі не обробляється.
   - `main_message_dispatcher()` перевіряє `user_data["profile"]` та стан бота; невідомі повідомлення переходять до fallbacks.

---

## 3. Jira вебхуки

### 3.1. Підсистема безпеки (`security_middleware`)

- Перевірка білих списків IP (`is_ip_in_whitelist`), включно з CIDR.
- Ліміт частоти (`check_rate_limit`) — зберігає історію звернень, чорний список із TTL.
- Виключення `/telegram` з перевірок (Telegram має динамічні IP). 
- **Валідація:**
   - Неправильний формат IP (`ValueError`) призводить до відхилення запиту.
   - Кожен запит перевіряє заголовки `X-Forwarded-For`/`X-Real-IP`; за їх відсутності використовується `request.remote`.
   - Якщо IP не у whitelist або перевищено ліміт — повертається відповідь 403/429 без передачі в handler.

### 3.2. Обробники подій

1. **`jira:issue_updated`** — `handle_issue_updated()` перетворює зміни статусу у повідомлення Telegram.
   - Фільтр технічного акаунта `JIRA_REPORTER_ACCOUNT_ID`.
   - Захист від дублікатів (`is_duplicate_message`, `add_message_to_cache`).
    - **Валідація:**
       - Відсутність ключа задачі або статусу викликає раннє завершення без відправлення повідомлення.
       - Некоректні дані від Jira логуються з `exc_info=True`.

2. **`comment_created`** — `handle_comment_created()`
   - Ігнорує коментарі бота та згенеровані ним авторські заголовки (`*Ім'я:`).
   - Чекає 5 секунд для отримання подій `attachment_created`.
   - Розширений пошук файлів у кешах (`PENDING_ATTACHMENTS_CACHE`, `ATTACHMENT_ID_CACHE`).
   - Відправляє повідомлення в Telegram, додає посилання на файли.
    - **Валідація:**
       - Порожні коментарі без вкладень пропускаються.
       - Якщо не знайдено користувача Telegram — повідомлення не відправляється.

3. **`attachment_created`** — зберігає дані у кеш, пробує знайти відповідну задачу, готує для запізнілого коментаря.
    - **Валідація:** перевіряє наявність `issue_key`/`attachment.id`; некоректні події логуються з попередженням та не обробляються далі.

4. **`jira:issue_created`** — `handle_issue_created()` форматує дані (`format_issue_info`), надсилає користувачу.
    - **Валідація:**
       - Перевіряє, чи знайдено користувача за `issue_key`; у разі відсутності завершує обробку.
       - Форматування відбувається лише за наявності ключових полів (summary, status).

5. **Інформаційні події** — логуються без дій (`issue_property_set`, `issuelink_created`, ...).
    - **Валідація:** необовʼязкові поля перевіряються на існування перед логуванням для запобігання `KeyError`.

### 3.3. Взаємодія з Telegram

- `send_telegram_message()` викликає `get_application()` із `main.py` для доступу до `application.bot`.
- `/telegram` вебхук Telegram Bot API обробляє `application` (запущений у `main.py`).

---

## 4. Cron‑логіка та повтори

- Основний цикл у `main()` кожні 10 хвилин викликає `user_manager.sync_pending_users()`.
- Бот забезпечує одиночний запуск через `acquire_lock()` та `kill_existing_bot_processes()`.
- `reset_webhook()` очищує телеграм-пул від непрочитаних оновлень перед стартом.
- **Валідація:**
   - `acquire_lock()` перевіряє наявність існуючого lock-файла, знімає прострочені блокування, при невдачі завершує запуск.
   - `kill_existing_bot_processes()` впевнюється, що не завершить поточний процес (`pid != current_pid`).
   - У циклі перевіряється результат синхронізації; винятки логуються і не зупиняють основну петлю.

---

## 5. Виявлені логічні ризики та прогалини

| № | Сценарій | Поточна поведінка | Ризик | Рекомендація |
|---|----------|-------------------|-------|--------------|
| 1 | Надання чужого контакту | `global_contact_handler()` не перевіряє `contact.user_id` | Можливість авторизуватися під чужим номером | Відхиляти контакт, якщо `contact.user_id != update.effective_user.id` |
| 2 | Оновлення username | `authorize_user()` записує порожній username в Google | Втрачається актуальний `telegram_username` | Передавати `update.effective_user.username` у `update_user_telegram()` |
| 3 | Флаг `google_available` | Після першого збою назавжди лишається `False` | Подальша синхронізація ніколи не виконується | Додавати повторні спроби або таймер для скидання флагу |
| 4 | `get_user_current_task()` | `str(None)` → `'None'` | Показ фіктивної задачі в привітанні | Перевіряти тип перед конвертацією, повертати `""` |
| 5 | Затримка у вебхуках | `await asyncio.sleep(5)` блокує обробник | Під навантаженням черга коментарів накопичується | Перейти на неблокуючий механізм (асинхронний пошук або фонові задачі) |
| 6 | Telegram webhook setup | Помилка `set_webhook()` лише логуються | Бот працює без вхідних повідомлень Telegram | Додати повторні спроби з експоненційним backoff та алерт |
| 7 | Відсутній контроль введення емодзі в полях | Поля `division/department/service` чутливі до емодзі | Некоректне значення в Jira | Застосувати whitelist значень або нормалізацію |
| 8 | Ручне редагування `user_states` | Файли JSON можуть бути пошкоджені | Винятки при `json.load` → повернення `None` | Додати схеми/валідацію та самоочищення |
| 9 | Повторні команди `/start` під час діалогу | Скидання `context.user_data` може стерти зібрані дані | Втрата прогресу у багатокроковій реєстрації | Вести стан діалогу окремо від профілю, обмежити `/start` |
| 10 | Rate limiting Telegram | Відправка великої кількості повідомлень (наприклад, Jira серія) | Можливий блок Telegram API | Додати throttling або чергу відправки |

---

## 6. Ідеї для автоматичних тестів

1. **Авторизація**
   - Передача власного контакту → успіх.
   - Передача чужого контакту → очікуваний виняток після впровадження перевірки.
   - Google Sheets недоступний → дані з кешу, `google_available` ставиться у `False`, після відновлення — скидається.

2. **Реєстраційний цикл**
   - Валідація телефону (коректний, короткий, символи, інші країни).
   - Автовидалення старих станів (24 години).
   - Завершення реєстрації → `registration_completed`, `BotState.AUTHORIZED_NO_TASKS`.

3. **Створення задачі**
   - Сценарій з відкритою задачею → заборона створення.
   - Проходження усіх кроків ConversationHandler з поверненням на головну.
   - Формування payload Jira, обробка кастомних полів, повторні спроби без `customfield_*`.

4. **Коментарі та вкладення**
   - Текстовий коментар → `add_comment_to_jira()` із підтвердженням.
   - Вкладення файлів → `attach_file_to_jira()`, `add_comment_with_file_reference_to_jira()`.
   - Jira webhook `comment_created` з кількома вкладеннями → знаходження у кеші, доставка в Telegram.

5. **Синхронізація та кеш**
   - `/sync_cache`: користувач із `sync_with_google=False` стає `True` після успіху.
   - `/cache_status`: повертає актуальний стан із `last_sync`.

6. **Вебхуки**
   - `jira:issue_updated`: зміна статусу → відправка повідомлення, унікальність через кеш.
   - `attachment_created` без `comment_created`: елемент у `ATTACHMENT_ID_CACHE`, час життя.
   - rate limiting: звернення більше ліміту → 429, розблокування після TTL.

---

## 7. План наступних кроків

1. **Впровадити логічні виправлення (див. таблицю ризиків).**
2. **Побудувати набір юніт та інтеграційних тестів:**
   - Використати мок-обʼєкти для Google Sheets, Jira API, Telegram Bot.
   - Створити сценарії «end-to-end» для основних користувацьких потоків.
3. **Налаштувати CI (GitHub Actions) для запуску тестів та lint.**
4. **Додати моніторинг помилок (Sentry / логування) для раннього виявлення збоїв.**
5. **Документувати процес відновлення після збоїв (Google Sheets, Jira API, Telegram webhook).**

---

## 8. Висновок

Поточна архітектура бота поєднує багатокрокову взаємодію в Telegram, гібридне зберігання даних (Google Sheets і локальний кеш) та глибоку інтеграцію з Jira. Вона покриває більшість робочих сценаріїв, але містить кілька логічних прогалин та місць для покращення стабільності. Запропоновані зміни й майбутні автоматичні тести допоможуть перетворити цей звіт у план дій для підвищення якості та надійності системи.
